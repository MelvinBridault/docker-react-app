# Hey, voici mon app sous Docker !

Envie de la lancer en mode dev ? C'est simple, tape juste :
docker-compose -f docker-compose.dev.yml up --build

Pour accéder à l'application :
http://localhost:8080

Bon code !
